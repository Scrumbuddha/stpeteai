# Copy this file to .env.local and fill in your key.
# Get a key at https://platform.claude.com/ (API Keys).
# Only needed for the AI Coach (/coach) — every other page works without it.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the Coach's model (defaults to claude-opus-4-8)
# COACH_MODEL=claude-haiku-4-5
