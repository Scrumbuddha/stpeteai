import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "St. Pete AI — AI Community in St. Petersburg",
  description:
    "St. Pete AI is a community of AI practitioners, researchers, and enthusiasts in St. Petersburg, Florida. Join us for meetups, workshops, and speaker events.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased font-sans`}>
        {children}
      </body>
    </html>
  );
}
